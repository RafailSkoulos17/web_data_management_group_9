1) create any number of ubuntu EC2 instances
2)  connect with ssh (create or use existing key) to the instances
3) sudo apt update
4) sudo apt install openjdk-8-jdk
5) sudo apt install python -y
6) echo "deb http://www.apache.org/dist/cassandra/debian 311x main" | sudo tee -a /etc/apt/sources.list.d/cassandra.sources.list
7) curl https://www.apache.org/dist/cassandra/KEYS | sudo apt-key add -
8) sudo apt-get update
9) sudo apt-get install cassandra
11) sudo vim /etc/cassandra/cassandra.yaml (to make the following changes)

    cluster_name: '<your-cluster-name>'

    ...

        - class_name: org.apache.cassandra.locator.SimpleSeedProvider
      parameters:
          # seeds is actually a comma-delimited list of addresses.
          # Ex: "<ip1>,<ip2>,<ip3>"
          - seeds: "<seed-private-ip>"  # Will be the same across all nodes. e.g., format “1.2.3.4”

    ...

    listen_address: <local-private-ip> # E.g. format: 1.2.3.4
    # No quotations around the DNS and you don’t need the tailing .us-west-2.compute.internal

    ...

    rpc_address: 0.0.0.0

    ...

    broadcast_rpc_address: <local-private-ip> # E.g. format: 1.2.3.4
    # No quotations around the dns and you don’t need the tailing .us-west-2.compute.internal

    ...

    endpoint_snitch: Ec2Snitch

    ...

12) sudo service cassandra stop
13) sudo rm -rf /var/lib/cassandra/data/system/*
14) open ports 7000, 7001, 7199, 9042, 9160 for custom TCP rule and 80 for http on the instances
15) sudo service cassandra start
16) nodetool status (to check if all the nodes are running)