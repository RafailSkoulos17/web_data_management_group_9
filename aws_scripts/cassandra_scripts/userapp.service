[Unit]
Description=Gunicorn user service
After=network.service

[Service]
User=ubuntu
Group=www-data
WorkingDirectory=/home/ubuntu/userapp
ExecStart=/usr/bin/gunicorn3 --workers 2 --bind unix:userapp.sock -m 007 app:app
