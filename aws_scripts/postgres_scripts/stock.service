[Unit]
Description=Gunicorn stock service
After=network.service

[Service]
User=ubuntu
Group=www-data
WorkingDirectory=/home/ubuntu/stockapp
ExecStart=/usr/bin/gunicorn3 --workers 2 --bind unix:stockapp.sock -m 007 app:app
Restart=always