[Unit]
Description=Gunicorn payment service
After=network.service

[Service]
User=ubuntu
Group=www-data
WorkingDirectory=/home/ubuntu/paymentapp
ExecStart=/usr/bin/gunicorn3 --workers 2 --bind unix:paymentapp.sock -m 007 app:app
Restart=always